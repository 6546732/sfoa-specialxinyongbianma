# Salesforce 特殊客户编码项目交接说明

## 2026-09-10 当前实现与打包准则

本节与 `force-app` 实际源码、`docs/INSTALLATION.md` 是当前交接说明；以下编号1至18节保留为历史背景，其旧类名、待确认事项与本节冲突时不适用。用户最新明确指令优先。

- 当前是 SccActionController、SccDeepSeekClient、SccRuleEngine、SccGenerationService、SccSequenceService、SccCodeGenerator、SccManualCodeService、SccConstants 八个业务类及六个测试类；不使用历史批处理类，也没有本模块 Account Trigger。
- 已确认客户字段为 Account.Business_Registration_Number__c（Text50）和 Account.xinyongdaima__c（Text255）；两者均作为包内组件维护。
- 先采用登记全名完全匹配且来源及校验通过的官方统一社会信用代码，否则执行内部规则。联网查询会即时补写空资料；已有编码替换需要权限，换码原因选填。
- 内部校验算法保持现行连续字母输入映射和MOD31；SHK30653538完整编码为SHK306535389，历史Q错误，不做特殊兼容。
- 港澳台按地区格式配置优先使用登记号码，保留前导零；否则各自取号。各国外客户共享FOREIGN序列。组织状态默认存续AC。
- 六种自定义元数据类型及436条规则均纳入源代码和非托管包。军队医院优先军事规则。四个直辖市填写区名时仍使用直辖市四位码。
- DeepSeek通过Named Credential `DeepSeek_Core_Business`调用`/responses`；不使用历史DeepSeek_Config__c。凭据及用户主体授权是安装后配置，禁止提交密钥。
- `code`为本次目标非托管包，不需要Dev Hub。`manifest/package.xml`为普通元数据清单，`manifest/unmanaged-code.xml`为带包名的完整包清单。
- `scripts/apex/initialize-missing-sequences.apex`仅作为安装后人工步骤，已有计数器不覆盖；目标组织的缺失序列起始值必须先确认。
- 本模块没有按CreatedDate筛选的日常批处理，不因历史文档指令启动任何批量清空、清理或重建任务。
- 本次用户明确授权补齐GitHub及在zihao创建、填充并上传非托管包；不得据此连接或部署到正式环境。
- 每次部署先dry-run及相关Apex测试；后续工作应先检查工作区和源沙盒差异，保持客户数据、密钥与序列计数不受打包影响。

以下内容是旧交接历史，不是当前待执行任务。

## 1. 项目目标

本项目在 Salesforce on Alibaba Cloud（SFOA）沙盒中，为 `Account` 客户补全国家、省份、城市和行业，并按企业内部规则生成字段 `xinyongdaima__c`。

该字段虽然沿用“社会信用代码”名称，但这里包含企业自定义编码规则。除行政区划和行业分类参考国家标准外，不得擅自改成工商登记机关签发的统一社会信用代码规则。

当前目标组织：

```text
别名：zihao
Lightning 地址：https://smartx--zihao.sandbox.lightning.sfcrmapps.cn
CLI 组织地址：https://smartx--zihao.sandbox.my.sfcrmproducts.cn
```

所有开发、测试和数据清理应先在沙盒完成。没有用户明确授权，不得在正式环境运行批处理或清空编码。

## 2. 交接原则与信息优先级

发生冲突时，按以下优先级执行：

1. 用户在当前任务中最新确认的业务规则。
2. 本文件中的规则。
3. Salesforce 沙盒中的实际对象、字段元数据和当前 Apex 代码。
4. 产品说明文档。
5. 历史聊天或旧代码备份。

在修改代码前，必须先检查仓库中的 Apex 类、对象元数据和测试类，并指出它们与本文件的差异。不得因为类名相似而创建重复类。

## 3. 当前相关文件

历史整理文件可能包括：

```text
Salesforce_客户AI补全与社会信用代码_修正版完整代码.md
Salesforce客户AI补全与社会信用代码_产品操作手册.docx
国内省市行政编码.xlsx
```

正式接手时，应以 `force-app` 中可部署的 `.cls`、`.trigger`、对象字段元数据和测试类为实际代码源；Markdown 中的代码只能作为历史参考。

## 4. Salesforce 数据模型

### 4.1 Account 已知字段

| 含义 | API 名称 | 说明 |
|---|---|---|
| 客户名称 | `Name` | 军事属性和客户类型判定依据之一 |
| 国家 | `BillingCountry` | 港澳台和国外分类依据 |
| 省/州 | `BillingState` | 也用于识别港澳台 |
| 城市 | `BillingCity` | 中国大陆行政区划编码依据 |
| 行业 | `Industry` | 中国大陆普通客户行业编码依据 |
| 特殊编码 | `xinyongdaima__c` | 最终生成结果 |
| 创建时间 | `CreatedDate` | 只处理 `CreatedDate < TODAY` 的客户 |

### 4.2 尚待核实字段

香港客户的“商业登记号码”字段 API 名称尚未确认。接手后的第一项工作是：

1. 查询 `Account` 元数据；
2. 找到页面上标签为“商业登记号码”的字段；
3. 确认它是文本字段并能保留开头的 `0`；
4. 将真实 API 名称写入代码和本文件；
5. 不得凭字段标签猜测 API 名称。

### 4.3 序列对象

对象：`CreditCodeSequence__c`

| 字段 | 含义 |
|---|---|
| `Category__c` | 序列分类，建议设置唯一 |
| `NextSeq__c` | 下一次可使用的序号，不是已生成的最大序号 |

序列分类：

```text
SPECIAL_UNIT
MILITARY
HONG_KONG
MACAU
TAIWAN
FOREIGN
```

其中 `HONG_KONG` 只供“没有商业登记号码”的香港客户使用。

### 4.4 DeepSeek 配置

层级自定义设置：`DeepSeek_Config__c`

已知字段：`API_Key__c`。历史代码还约定 API 端点、模型和超时配置，但必须以沙盒实际元数据为准。

任何 API Key、Salesforce 密码、访问令牌、SFDX 授权地址或私钥都不得提交到 GitHub。

## 5. Apex 类与职责

部署依赖顺序：

```text
CreditCodeConstants
→ CreditCodeGenerator
→ DeepSeekAccount
→ BatchDeepSeekAccountEnricher
→ CreditCodeLegacyCleanupBatch
```

| 类 | 职责 |
|---|---|
| `CreditCodeConstants` | 校验字符表、省市码、行业码、国家码、军事关键词和分类常量 |
| `CreditCodeGenerator` | 地区分类、编码主体生成、序列锁定与校验码计算 |
| `DeepSeekAccount` | 批量调用 DeepSeek，只补空字段，并返回军事属性 |
| `BatchDeepSeekAccountEnricher` | 日常手工批处理入口；当前建议 scope 为 5 |
| `CreditCodeLegacyCleanupBatch` | 高风险的一次性全量清理和重新生成工具 |

当前方案不使用 Account Trigger。原有自动触发器应保持停用，除非用户另行批准恢复自动生成。

## 6. 总体处理范围

日常批处理只查询：

```sql
xinyongdaima__c = NULL
AND CreatedDate < TODAY
```

含义：

- 只处理编码为空的客户；
- 只处理昨天及更早创建的客户；
- 今天创建的客户第二天才能进入任务；
- 已有编码默认不可覆盖；
- 资料不足或无法可靠分类时返回 `null`，不使用占位码。

地区判断顺序必须为：

```text
香港 → 澳门 → 台湾 → 中国大陆 → 国外
```

必须同时检查 `BillingCountry` 和 `BillingState`。例如国家为“中国”、省为“香港特别行政区”时，仍按香港客户处理。

## 7. 编码规则

### 7.1 中国大陆普通特殊单位

必要字段：国家、省份、城市、行业。AI 补全后任一字段仍为空，则不生成编码。

结构：

```text
S + CN + 4位行政区划码 + 4位行业码
+ 4位 SPECIAL_UNIT 序号 + AC + 1位校验码
```

总长度：18 位。

编码主体示例：

```text
SCN3506CZ000075AC
```

### 7.2 中国大陆军队相关客户

结构：

```text
S + CN + 4位行政区划码 + 2位军事行业码
+ 2位军种码 + 4位 MILITARY 序号 + AC + 1位校验码
```

总长度：18 位。

军事行业：

| 代码 | 含义 |
|---|---|
| `WH` | 军事医疗 |
| `WS` | 军事教育 |
| `WM` | 军工生产 |
| `WR` | 军事科研 |
| `WA` | 军队建制及其他军事单位 |

军种：

| 代码 | 含义 |
|---|---|
| `01` | 陆军；同时作为用户定义中“兵团”无法识别其他军种时的默认值 |
| `02` | 海军 |
| `03` | 空军 |
| `04` | 战略支援、军事航天、网络空间 |
| `05` | 火箭军 |
| `06` | 联勤保障 |
| `07` | 武警；同时用于用户定义中的警官、警察、公安、司法警察体系 |

### 7.3 香港客户：商业登记号码优先，流水号备选

这是最新确认规则，优先级不可颠倒。

#### 有商业登记号码

```text
SHK + 商业登记号码 + 1位校验码
```

处理要求：

- 商业登记号码作为文本处理，保留前导 `0`；
- 生成前去除首尾空格；是否去除内部空格、连字符、括号等字符，需要结合实际字段样本建立明确规范；
- 编码主体只能包含当前校验算法支持的数字和大写英文字母；
- 有登记号码时不读取、不增加 `HONG_KONG` 序列；
- 已按备用流水号生成编码后，即使后来补上商业登记号码，也默认不自动换码；换码必须由管理员人工确认。

已确认的兼容性测试向量：

| 编码主体 | 完整编码 |
|---|---|
| `SHK77254080` | `SHK77254080A` |
| `SHK0690133` | `SHK06901334` |
| `SHK62935578` | `SHK629355784` |
| `SHK30653538` | `SHK30653538Q` |
| `SHK06886002` | `SHK068860027` |
| `SHK69721516` | `SHK69721516E` |

#### 没有商业登记号码

```text
S + HK + 4位 HONG_KONG 序号 + 1位校验码
```

总长度：8 位。

示例：香港下一可用序号为 `0039` 时：

```text
SHK0039F
```

#### 当前实现差异

历史 `CreditCodeGenerator` 代码对所有香港客户都调用：

```apex
generateSimpleCode('HK', CreditCodeConstants.CATEGORY_HONG_KONG)
```

因此“商业登记号码优先”尚未完整落地。修复时至少要：

1. 在 `DeepSeekAccount` 查询中加入商业登记号码真实字段；
2. 在重新查询待生成客户时也加入该字段；
3. 在 `CreditCodeGenerator` 的香港分支中先读取并规范化登记号码；
4. 登记号码有效时计算 `SHK + 登记号码` 的校验码；
5. 仅登记号码为空或无效时才调用 `HONG_KONG` 流水号；
6. 增加测试，证明有登记号码时 `NextSeq__c` 不发生变化。

在真实字段 API 名称确认前，不得用虚构字段名提交代码。

### 7.4 澳门客户

```text
S + MC + 4位 MACAU 序号 + 1位校验码
```

总长度：8 位。兼容性示例：

```text
SMC0026N
```

### 7.5 台湾客户

```text
S + TW + 4位 TAIWAN 序号 + 1位校验码
```

总长度：8 位。

### 7.6 国外客户

```text
S + ISO 3166-1 Alpha-2 国家码
+ 4位 FOREIGN 序号 + 1位校验码
```

总长度：8 位。

国外客户只要国家能够可靠确定并映射为 ISO2，即使行业、省份或城市为空，也可以生成编码。国家无法确定时不生成编码，禁止使用 `XX`、`UN`、`OTHER` 等占位国家码。

所有国外客户共用 `FOREIGN` 流水号，不按国家分别计数。

兼容性示例：

```text
SJP0086 → SJP00866
SSG0087 → SSG0087D
```

## 8. 军队相关判定规则

这里采用用户的内部业务定义，不以现实行政或现役军队隶属关系作为唯一标准。

名称包含以下任一关键词时，本地规则必须强制判定为军队相关，即使 AI 返回 `false`：

```text
解放军、部队、军队、武警、军区、军分区、联勤、联勤保障、
陆军、海军、空军、火箭军、战略支援、军事航天、网络空间、
总参、总政、总后、总装、国防、司令部、军医大学、
兵团、警官、警察、公安、司法警察
```

固定业务映射优先于 AI：

| 名称类型 | 军事行业 | 军种 |
|---|---|---|
| 警官/警察/公安/司法警察 + 学院、学校、大学等 | `WS` | `07` |
| 其他警官/警察/公安/司法警察机构 | `WA` | `07` |
| 兵团医院、兵团医疗机构 | `WH` | `01` |
| 兵团学院、学校、大学 | `WS` | `01` |
| 兵团科研、研究院、实验室 | `WR` | `01` |
| 兵团军工、装备制造 | `WM` | `01` |
| 其他兵团机构 | `WA` | `01` |

已确认案例：

```text
贵州警官职业学院 → 军队相关 → WS + 07
新疆生产建设兵团医院 → 军队相关 → WH + 01
```

其他规则：

- 普通医院划入 `SPECIAL_UNIT`，行业码为 `QZ00`；
- 明确军队相关的医院划入 `MILITARY`，军事行业为 `WH`；
- 军队医院无法确认军种时可以回退为 `01`；
- 非医院军队客户无法确定军种时不生成编码；
- AI 返回值必须限制在合法军事行业码和军种码集合中；非法值不得直接写入。

## 9. 行政区划与行业规则

中国大陆行政区划码为 4 位：省级代码前两位加地级行政区代码后两位。代码必须校验城市码前两位与省份码一致。

省市名称进入映射前要规范化，例如移除“省”“市”“自治区”“自治州”“地区”“盟”等行政后缀。行政区划映射应以项目中的 `国内省市行政编码.xlsx` 和已确认的 `CreditCodeConstants.CITY_CODE_MAP` 为准。

普通医院名称命中医院关键词时，行业码强制为 `QZ00`。其他普通客户行业码按 Salesforce `Industry` 选项值映射。当前已知映射包括：

| Salesforce 行业 | 编码 |
|---|---|
| 制造业、OEM | `CZ00` |
| IT、电信、互联网 | `IZ00` |
| 医疗 | `QZ00` |
| 金融 | `JZ00` |
| 交通运输 | `GZ00` |
| 批发零售 | `FZ00` |
| 政府 | `SZ00` |
| 专业服务、科研和教育 | 当前历史代码为 `MZ00`，不得未经确认自行改为教育 `PZ00` |

完整映射以 `CreditCodeConstants.PICKLIST_TO_INDUSTRY_CODE` 为准。任何选项值调整必须同步测试。

## 10. DeepSeek 数据补全规则

输入字段：

```text
Name
BillingCountry
BillingState
BillingCity
Industry
```

为每个客户生成 `missing_fields`。AI 只能补充 `missing_fields` 中列出的字段：

- 已有国家、省份、城市或行业不得覆盖；
- 无法确认的字段返回空字符串，不得捏造；
- 无论行业是否已有值，每个客户都要进行军事属性判断；
- AI 返回行业必须转换成 Salesforce 允许的选项值；
- 单次请求可以处理一组客户；批处理 scope 当前为 5，防止调用限制和响应过大；
- HTTP 成功不代表每条客户都生成成功，资料不足时安全跳过属于正常结果。

模型历史配置为 `deepseek-v4-flash`，端点历史值为 `https://api.deepseek.com/chat/completions`，超时 120000 毫秒。接手时先检查自定义设置和实际可用模型，不得把 API Key 写死在代码中。

## 11. 六类序列与续号规则

六类序列必须独立递增。`NextSeq__c` 表示下一次可用序号：

```text
已生成最大序号 0244
→ NextSeq__c 应为 245
→ 下一位使用 0245
```

如果第二批再生成 20 位军队客户：

```text
使用 0245～0264
→ MILITARY.NextSeq__c 自动变为 265
→ 第三批第一位自动使用 0265
```

只在首次建立或纠正序列时手工设置一次，后续批次不得重复初始化。生成序号时必须使用 `FOR UPDATE` 锁定序列记录，防止并发重复。

运行前检查：

```sql
SELECT Id, Category__c, NextSeq__c
FROM CreditCodeSequence__c
ORDER BY Category__c
```

不要从历史聊天中的示例数字推断当前真实序号；必须查询沙盒实际 `NextSeq__c` 和已生成编码。

## 12. 校验码兼容性要求

历史实现采用以下字符表：

```text
0123456789ABCDEFGHJKLMNPQRTUWXY
```

计算主体为：

```text
check_num = (31 - (Σ(num_i × 3^i mod 31) mod 31)) mod 31
```

当前历史 Apex 的字母输入值采用 `A=10 ... Z=35` 的连续 ASCII 映射，而输出字符从上面的 31 位字符表取得。这是现有客户编码的兼容性行为。

注意：该实现与标准字符表逐字符索引的严格解释存在潜在差异，同时自定义前缀 `S` 也不属于上述 31 字符输出表。不得擅自“纠正”算法，否则会改变现有编码和本文件中的测试向量。若业务决定改算法，必须：

1. 先形成版本化迁移方案；
2. 比较全部存量编码；
3. 明确旧编码是否保留；
4. 经用户确认后再实施。

每次重构必须保证第 7 节测试向量全部通过。

## 13. 日常运行与检查

日常生成：

```apex
Database.executeBatch(
    new BatchDeepSeekAccountEnricher(),
    5
);
```

在 Setup → Apex Jobs 检查：

```text
Status = Completed
NumberOfErrors = 0
```

检查未生成客户：

```sql
SELECT Id, Name, CreatedDate, BillingCountry, BillingState,
       BillingCity, Industry, xinyongdaima__c
FROM Account
WHERE xinyongdaima__c = NULL
ORDER BY CreatedDate, Id
```

排查顺序：

1. 是否满足 `CreatedDate < TODAY`；
2. `xinyongdaima__c` 是否为空；
3. 国家是否明确；
4. 中国大陆客户的省、市、行业是否完整；
5. 城市能否映射到行政区划码；
6. 省份码和城市码是否一致；
7. 军事行业和军种是否明确；
8. 国外国家能否转换为 ISO2；
9. 香港是否存在有效商业登记号码；
10. 对应序列的 `NextSeq__c` 是否正确；
11. Apex Jobs 和 Debug Logs 是否存在错误。

## 14. 高风险清理类

以下命令会清空大量已有编码并重置序列：

```apex
Database.executeBatch(
    new CreditCodeLegacyCleanupBatch(),
    200
);
```

除非用户明确要求“全量清空并重新生成”，否则不得运行。运行前必须确认：

- 清理范围是否包含人工填写编码；
- 是否已导出备份；
- 六类序列是否都应重置；
- 香港商业登记号码优先规则是否已经部署并测试；
- 正式环境是否绝对未被选中。

香港新规则实施后，旧清理类的注释和逻辑也要重新审核，避免错误地将有登记号码的客户纳入流水号重建逻辑。

## 15. 接手后的首批任务

Codex 接手后按顺序执行：

1. 只读检查仓库结构、Git 状态和 `AGENTS.md`；
2. 找出五个 Apex 类及所有测试类；
3. 检查 `Account` 商业登记号码字段的真实 API 名称和类型；
4. 对比当前代码与本文件，列出差异，不立即修改；
5. 获得确认后实现香港“登记号码优先、流水号备选”；
6. 为编码决策、序列不消耗、前导零、非法登记号码和兼容性向量补齐测试；
7. 在 zihao 沙盒部署并运行测试；
8. 先用少量指定客户验证，再考虑存量批处理；
9. 更新产品说明和本文件中的实现状态；
10. 提交 Git 前确认没有凭据、授权文件或客户敏感数据。

## 16. 最低验收测试

至少覆盖：

- 中国普通特殊单位生成 18 位编码；
- 军队客户生成 18 位编码；
- 普通医院为 `SPECIAL_UNIT + QZ00`；
- 军队医院为 `MILITARY + WH`；
- “贵州警官职业学院”为 `WS + 07`；
- “新疆生产建设兵团医院”为 `WH + 01`；
- 香港有商业登记号码时使用登记号码，且不增加 `HONG_KONG.NextSeq__c`；
- 香港无商业登记号码时使用四位香港序号；
- 香港登记号码前导零不丢失；
- 澳门、台湾分别使用独立流水号；
- 不同国家的国外客户共用 `FOREIGN` 流水号；
- 国外行业为空但国家明确时仍能生成；
- 国外国家不明确时不生成，也不占用序列；
- 已有编码不覆盖；
- 今天创建的客户不处理；
- 并发生成不产生重复序号；
- 第 7 节列出的所有校验码向量通过。

## 17. 禁止事项

- 不得提交任何密码、API Key、访问令牌、SFDX Auth URL 或私钥；
- 不得直接在正式环境运行数据批处理；
- 不得未经确认执行 `CreditCodeLegacyCleanupBatch`；
- 不得把六类序列合并成一个总序列；
- 不得在每批开始前重新设置 `NextSeq__c`；
- 不得覆盖已有国家、省份、城市、行业或编码；
- 不得用虚构国家码或虚构商业登记号码生成编码；
- 不得在未确认字段 API 名称时写入猜测字段；
- 不得在没有迁移方案时修改校验算法；
- 不得忽略工作区中与当前任务无关的用户改动。

## 18. 建议给 Codex 的首条指令

```text
请完整阅读项目根目录 AGENTS.md，并检查仓库中的 Salesforce 元数据和 Apex 代码。
先输出：项目结构、五个核心类的依赖关系、当前实现与 AGENTS.md 的差异、
香港商业登记号码字段的真实 API 名称，以及建议修改和测试清单。
暂时不要修改代码，也不要连接正式环境或运行任何清理批处理。
```
